FROM oven/bun:1.4.0
WORKDIR /app
COPY . .
RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg && rm -rf /var/lib/apt/lists/*
RUN bun install --frozen-lockfile
CMD ["bun", "packages/queue/src/worker.ts"]
